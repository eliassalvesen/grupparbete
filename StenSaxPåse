import random

print("*" * 15 + "\nSten -Sax -Påse" + "\n" + "*" * 15)

def main():
    

def checkResults(user, computer):
    # checkResults(user,computer) som tar användarens och datorns val som argument och returnerar ett värde för att
    # ange vem som vann eller om det blev oavgjort.

    return x

slumptalsGeneratorn  = random.randint(1,3)
